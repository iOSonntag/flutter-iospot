


import 'package:flutter/material.dart';
import 'package:iospot/_packages/extended_theme/theme_data_extension.dart';


class SpaceXS extends StatelessWidget {

  const SpaceXS({
    super.key
  });

  @override
  Widget build(BuildContext context)
  {
    double size = Theme.of(context).dimensions.spaceXS;
    return SizedBox(
      width: size,
      height: size,
    );
  }
}

class SpaceS extends StatelessWidget {

  const SpaceS({
    super.key
  });

  @override
  Widget build(BuildContext context)
  {
    double size = Theme.of(context).dimensions.spaceS;
    return SizedBox(
      width: size,
      height: size,
    );
  }
}

class SpaceM extends StatelessWidget {

  const SpaceM({
    super.key
  });

  @override
  Widget build(BuildContext context)
  {
    double size = Theme.of(context).dimensions.spaceM;
    return SizedBox(
      width: size,
      height: size,
    );
  }
}

class SpaceL extends StatelessWidget {

  const SpaceL({
    super.key
  });

  @override
  Widget build(BuildContext context)
  {
    double size = Theme.of(context).dimensions.spaceL;
    return SizedBox(
      width: size,
      height: size,
    );
  }
}

class SpaceXL extends StatelessWidget {

  const SpaceXL({
    super.key
  });

  @override
  Widget build(BuildContext context)
  {
    double size = Theme.of(context).dimensions.spaceXL;
    return SizedBox(
      width: size,
      height: size,
    );
  }
}